.vscode/**
.vscode-test/**
.gitignore
vsc-extension-quickstart.md

# Only ship the VS Code theme JSON inside the vsix — exclude the other dist
# subdirs (npm pkg, Windows Terminal scheme, the vsix output itself).
dist/**
!dist/vscode-theme/**
